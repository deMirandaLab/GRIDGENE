# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"
  commands:
    # Removed: - pip install .[docs]  <-- This line caused the error
    - pip install -r docs/requirements.txt # <--- Keep this line, it's correct

sphinx:
  configuration: docs/source/conf.py
